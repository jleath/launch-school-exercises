class Cat
end

# We can define a class using the `class` keyword followed by the name
# of the class. The name of the class should be written in CamelCase.
# We end the class body with the `end` keyword. The class defined above 
# has only the state and behaviors inherited from the `Object` class.